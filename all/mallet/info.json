{
  "title": "Mallet",
  "description": "Full map editor for creating games with, including brushes (boxes) and entities (objects) with a custom .map format (which can just be read as json)",
  "author": "Flufi",
  "version": "1.1",
  "genre": "Utility",
  "permissions": ["file viewer"],
  "supports":["originOS"]
}
